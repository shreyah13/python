n = int(input('Enter number of elements in list for sorting: '))
array = []
print('Enter list elements: ')
for i in range(n):
	value = int(input())
	array.append(value)
	


for i in range(0,n-1):
    sorted = True
	for j in range(0,n-i-1):
    count += 1
    if array[j] > array[j+1]:
        swap array[j] with array[j+1]
			sorted = False
	if sorted:
		break # breaks the outer loop

